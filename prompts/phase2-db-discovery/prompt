Document the database layer of the FaaS application. Analyze the IaC configuration files in the storage directory (e.g., cli-inputs.json, CloudFormation templates) to extract table names, primary keys, sort keys and GSIs. Supplement with Lambda function code analysis to infer non-key attributes from DynamoDB SDK operations (getItem, putItem, query, updateItem). For each table document: name, primary key structure, GSIs if any, and key attributes with their types.Produce a documented recommendation on migration scope: single-phase (backend + data together) or two-phase (backend first, data migration deferred). Include technical justification based on table count, data duplication patterns, and complexity.. Save the schema documentation to docs/output/data-base-definition.txt and an execution report to docs/log/phase2.report.
