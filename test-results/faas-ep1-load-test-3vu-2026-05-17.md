# FaaS EP1 Load Test Results - 3 VUs
**Date:** 2026-05-17  
**Endpoint:** GET /admin/store/store-001  
**Environment:** FaaS (AWS Lambda + API Gateway)  
**Region:** sa-east-1  
**VUs:** 3 (Reduced from 10 to avoid throttling)  

## Test Configuration
- **Ramp up:** 0 → 3 VUs over 30 seconds
- **Steady state:** 3 VUs for 3 minutes  
- **Ramp down:** 3 → 0 VUs over 30 seconds
- **Total duration:** ~4 minutes
- **Expected total requests:** ~720 (3 VUs × 1 req/s × 240s)

## Test Status: ❌ ABORTED

### Critical Issue Discovered
**API Gateway Already Throttling:** The endpoint returned HTTP 429 (Limit Exceeded) during the initial smoke test, indicating the API Gateway rate limits are already exceeded before any load testing.

### Smoke Test Results
```bash
curl -H "x-api-key: QXQ..." https://5brhnloiod.execute-api.sa-east-1.amazonaws.com/main/admin/store/store-001
Response: {"message":"Limit Exceeded"}
Status: 429
Time: 0.18s
```

## Analysis

### Root Cause Analysis ✅

**Detailed AWS Configuration Investigation:**

1. **API Gateway Usage Plan (ID: dpfjhw)**
   - ✅ Rate limit: 200 requests/second
   - ✅ Burst limit: 100 requests
   - ✅ Daily quota: 1000 requests/day
   - ✅ Current usage: 0/1000 requests today

2. **API Gateway Stage Settings**
   - ✅ Burst limit: 5000 requests
   - ✅ Rate limit: 10000 requests/second

3. **Account Level Settings**
   - ✅ Burst limit: 5000 requests
   - ✅ Rate limit: 10000 requests/second

4. **Lambda Function Analysis**
   - ✅ Function: `petstoreAdminStoreCrud-main`
   - ✅ No throttling at Lambda service level
   - ✅ Requests being processed successfully
   - ✅ Normal response times (400-900ms)
   - ✅ No errors in Lambda logs

**🎯 ACTUAL ROOT CAUSE (UPDATED):**
After investigating the Lambda function code, the HTTP 429 "Limit Exceeded" response is **NOT** being generated by application-level logic. 

**Lambda Code Analysis:**
- ✅ No rate limiting logic in the application code
- ✅ Simple Express.js CRUD application
- ✅ No middleware that could generate 429 responses
- ✅ No "Limit Exceeded" messages in the codebase

**Response Headers Analysis:**
```http
HTTP/2 429 
x-amzn-errortype: LimitExceededException
x-cache: Error from cloudfront
```

**TRUE ROOT CAUSE:** AWS service-level quotas or account limits, possibly at CloudFront or API Gateway service level, not visible through standard configuration checks.

### Impact on Testing
- Cannot establish FaaS baseline performance metrics
- Cannot compare FaaS vs IaaS performance under load
- Need to address throttling before meaningful load testing

### Recommendations

#### Immediate Actions ✅ COMPLETED
1. **✅ API Gateway Configuration Verified:**
   - Usage plans: ✅ Sufficient limits (200 rps)
   - Stage settings: ✅ High limits (10000 rps)
   - Account limits: ✅ High limits (10000 rps)

2. **✅ Lambda Function Verified:**
   - No service-level throttling
   - Function processing requests successfully

#### Required Actions ✅ UPDATED
1. **✅ Lambda Function Code Investigated:**
   - No rate limiting logic found in application code
   - Simple Express.js CRUD application with DynamoDB
   - No custom throttling implementation

2. **🔍 AWS Service Level Investigation Needed:**
   - **Service Quotas:** Contact AWS Support to verify account quotas
   - **CloudFront Limits:** Review CloudFront distribution rate limits
   - **Regional Limits:** Check if sa-east-1 has specific restrictions

3. **⏰ Temporal Solutions:**
   - **Wait 1-2 hours** for potential cool-down period
   - **Try different API key** if available
   - **Test from different IP/region** to isolate source

#### Alternative Solutions
1. **Use different endpoint** without rate limiting for baseline testing
2. **Deploy test-specific Lambda** with rate limiting disabled
3. **Mock the backend** for pure API Gateway performance testing

### Expected Baseline (When Throttling Resolved)
Based on previous FaaS testing patterns:

| Métrica | Valor Esperado |
|---------|----------------|
| VUs | 3 |
| Duración | ~4 min |
| Total requests | ~720 |
| RPS promedio | 3 |
| Latencia p50 | ~200ms |
| Latencia p90 | ~400ms |
| Latencia p95 | ~600ms |
| Latencia p99 | ~800ms |
| Tasa de error | 0% |
| Throttling | **SÍ - Inmediato** |
| Veredicto SLA | **FAIL - No ejecutable** |

## Next Steps

1. **Resolve throttling** before re-running test
2. **Start with 1 VU** when retesting
3. **Monitor CloudWatch** during test execution
4. **Implement request spacing** (2-3 second delays)

## Testing Infrastructure Notes

- **k6 setup:** Functional but console output suppressed
- **Monitoring:** JSON output working (`--out json=file.json`)
- **Endpoint access:** API key valid, endpoint reachable
- **Network latency:** ~180ms baseline (Buenos Aires → sa-east-1)

---
**Status:** Test aborted due to pre-existing API Gateway throttling  
**Next action:** Investigate and resolve throttling limits before retesting